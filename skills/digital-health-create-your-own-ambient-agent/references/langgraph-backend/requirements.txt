# LangGraph + LangChain (1.0 LTS — per langchain-dependencies skill)
langgraph>=1.0,<2.0
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langsmith>=0.3.0

# NVIDIA AI Endpoints
langchain-nvidia-ai-endpoints>=0.3.0

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# NeMo Guardrails — only if guardrails enabled in Phase 2(f)
# Pip package name is nemoguardrails (NOT nemo-guardrails)
# nemoguardrails
